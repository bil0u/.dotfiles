# - WARNING - PARSER SEEMS TO HAVE A PROBLEM WITH 's' AT END OF LINES
#
# - SYNTAX -
#
#   name:    package name
#   exists:  command to run to know if the package is installed
#   install: commands to run to install the package
#   steps:   number of commands in 'install' category
#   update:  command to update the package , 'false' if none
#
#   RESPECT THE INDENTATION !
#
--
requirements:
    -
        name: 'XCode'
        exists: xcode-select -p
        install:
            - xcode-select --install
        steps: 1
    -
--
modules:
    -
        name: 'Homebrew'
        exists: which brew
        install:
            - bash modules/brew/install
            - brew tap caskroom/cask
        steps: 2
        update: brew update
        uninstall: bash modules/brew/install
    -
        name: 'Powerline fonts'
        exists: false
        install:
            - bash modules/powerline/install.sh
        steps: 1
        update: false
        uninstall: false
    -
        name: 'Oh-My-Zsh'
        exists: echo $ZSH | grep oh-my-zsh
        install:
            - chsh -s /bin/zsh
        steps: 1
        update: bash modules/oh-my-zsh/tools/upgrade.sh
        uninstall: bash modules/oh-my-zsh/tools/uninstall.sh
    -
--
brew:
    packages:
        - 'coreutils'
        - 'wget'
        - 'cmake'
        - 'zsh-completions'
        - 'node'
        - 'python'
    casks:
        - 'iterm2'
        - 'atom'
--
app_prefs:
    -
        name: 'iTerm'
        type: plist
        file: iterm/com.googlecode.iterm2.plist
    -
