[default]
aws_access_key_id = {{ (onepasswordItemFields "AWS" .work.accounts.op.vault .work.accounts.op.account_id).keyId.value }}
aws_secret_access_key = {{ (onepasswordItemFields "AWS" .work.accounts.op.vault .work.accounts.op.account_id).secretKey.value }}
